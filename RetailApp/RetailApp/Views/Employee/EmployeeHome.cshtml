@model IEnumerable<RetailApp.Models.Schedule>

@{
    ViewBag.Title = "EmployeeHome";
}

<h2>EmployeeHome</h2>
@*<input type="button" value="Checkout" onclick="location.href='@Url.Action("Checkout", "Employee")'"/>*@
<div class="btn-group btn-group-justified">
    <a href="#" class="btn btn-primary btn-default" onclick="location.href='@Url.Action("Checkout", "Employee")'">Checkout</a>
    <a href="#" class="btn btn-primary btn-default" onclick="location.href='@Url.Action("Inventory", "Employee")'">Inventory</a>
    <a href="#" class="btn btn-primary btn-default" onclick="location.href='@Url.Action("MySales", "Employee")'">MySales</a>
</div>

<div>
    <table class="table table-hover">
        <tr class="info">
            <th>
                @Html.DisplayNameFor(model => model.StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndTime)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.StartTime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EndTime)
                </td>         
            </tr>
        }

    </table>
</div>
